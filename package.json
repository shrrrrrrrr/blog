{
  "name": "song-haoran-tech-blog",
  "version": "1.0.0",
  "private": true,
  "description": "宋浩然的个人技术博客，使用原生 HTML、CSS 和 JavaScript 构建。",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check script.js && node scripts/validate-content.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
