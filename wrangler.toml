# Cloudflare deployment configuration.
# The current Cloudflare dashboard flow uses `npx wrangler deploy`, so this
# file declares the static asset directory for Workers Static Assets.
name = "blog"
compatibility_date = "2026-08-13"
send_metrics = false

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
